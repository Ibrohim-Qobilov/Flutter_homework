import 'package:flutter/material.dart';

class PrayerCompossPage extends StatelessWidget {
  const PrayerCompossPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Prayer Composs"),),
    );
  }
}