import 'package:flutter/material.dart';

class PrayerMenuPage extends StatelessWidget {
  const PrayerMenuPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Prayer Menu"),),
    );
  }
}