import 'package:flutter/material.dart';

class PrayerSearchPage extends StatelessWidget {
  const PrayerSearchPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Prayer search"),),
    );
  }
}